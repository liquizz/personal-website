import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react' // or whatever plugin you're using

export default defineConfig({
  base: '/personal-website/', // This should match your repository name
})
